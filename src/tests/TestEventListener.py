import unittest
# from PaperTrader.EventListener import EventListener
# from unittest.mock import Mock, patch, MagicMock

class TestEventListener(unittest.TestCase):


    # @patch("multiprocessing.connection.recv")    
    def testPipeCall(self):
        assert True 
    #     paper_trader = Mock()
    #     sender = Mock()
    #     proc = Mock()
    #     recv.return_value = "I SHOULD HAVE ABORTED YOU"


    #     EventListener.listen(paper_trader, recv, sender, proc)
